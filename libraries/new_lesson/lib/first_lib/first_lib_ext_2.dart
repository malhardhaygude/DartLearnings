part of 'first_lib.dart';
// import 'dart:math';     // this line will throw error no imports allowed in part of.

class C {
  var num = 10;
  var num2 = math
      .pi; // math module can be accessed here by importing it in primary file.
}
